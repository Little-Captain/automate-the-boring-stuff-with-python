# 在 Python 中有一个值称为 None，它表示没有值
# None 是 NoneType 数据类型的唯一值
spam = print('Hello!')
print(None == spam)

# 对于所有没有 return 语句的函数定义，Python 都会在末尾加上 return None。
# 这类似于 while 或 for 循环隐式地以 continue 语句结尾。
# 如果使用不带值的 return 语句（也就是只有 return 关键字本身），那么就返回 None